package tests;


import static org.junit.jupiter.api.Assertions.assertEquals;


/*public class TestTeam {
    Team birds;
    Team braves;
    Player jack;
    Player john;
    List<String> playersBirds;
    List<String> playerBraves;
    Map<String, String> teamLevel = new HashMap<>();
    Team team = new Team();

    @Before
    public void runBefore(){
        birds = new Team();
        braves = new Team();
        jack = new Player();
        john = new Player();
    }


   /* public void testTime(){
        team.addTeam(birds);
        assertEquals(null, team.getPlayer(birds));
        jack.addPlayer(birds, jack);
        john.addPlayer(braves, john);
        assertEquals(jack, team.getPlayer(birds));
        john.addPlayer(birds, john);
        assertEquals(john, team.getPlayer(birds));
        assertEquals(john.getPlayer(birds), john);
        team.removeTeam(birds);
        assertEquals(null, team.getPlayer(birds));


        team.addTeam(birds);
        assertEquals(null, team.getPlayer(birds));
        team.addPlayer(birds, "Jacob");
        assertEquals(ArrayList<>("Jacob", team.getPlayer(birds));
        team.addPlayer(birds, "Jack");
        assertEquals(ArrayList<>("Jacob", "Jack"), team.getPlayer(birds));
        team.removeTeam(birds);
        assertEquals(null, team.getPlayer(birds));
        }
         */






